// Layout vacío. Se mantiene para evitar que expo-router cree el
// directorio (tabs) automáticamente. Los tabs reales viven en src/app/
// (raíz) e incluyen AppTabs al final.
export default function EmptyTabsLayout(): null {
  return null;
}
